# Life Dashboard 产品文档

**版本：** v0.1 · MVP 规划  
**定位：** 把日常行动变成可看见、可积累的个人进度。它不是待办清单，也不是强迫打卡工具；它是一套轻量、私人的“人生 RPG 面板”。

**产品方向：** 温和陪伴 + 人生洞察 + 少量 RPG 成长感。借鉴 Finch 的正向支持、Daylio 的数据洞察和 Habitica 的任务分类，但不复制惩罚、商城、战斗或社交竞争。

## 1. 要解决的问题

很多人知道要运动、学习、联系朋友、整理生活，却很难持续：任务分散，进步没有反馈，完成一件小事也没有“我在变好”的感受。

Life Dashboard 要让用户每天花 1–3 分钟完成一个循环：

```text
选择今天的主线行动 → 完成并勾选 → 获得 XP / 属性成长 → 看见长期变化与下一步建议
```

产品的成功不是让用户记录更多，而是让用户更愿意持续做真正重要的小事，并能理解自己的生活重心是否失衡。

## 2. 目标用户与使用场景

首版面向有自我提升习惯、喜欢游戏化反馈的个人用户。

典型场景：

- 早上：选定今天 3–5 个任务。
- 白天：完成后快速勾选，获得即时反馈。
- 晚上：查看今天 XP、连续天数与属性变化；为明天准备任务。
- 每周：观察五项人生属性是否失衡，并作出调整。

## 3. 产品原则

1. **先行动，后记录。** 首页必须能快速完成/新增任务，不能只是好看的报表。
2. **一件主线，少量支线。** 每日明确一个最重要任务，其余任务不超过 4 个。
3. **本地优先。** MVP 不要求注册，数据保存在浏览器 localStorage。
4. **反馈可感知，但不惩罚。** 有 XP、等级、徽章和连续记录；不扣血、不降级、不羞辱式提醒。
5. **洞察必须可行动。** 图表和周报必须导向一个小建议，而不是只展示数字。
6. **视觉服务于习惯。** RPG 风格用来强化进度感，不能遮蔽信息与操作。

## 4. MVP 功能范围

### P0：首发必须具备

| 功能 | 用户价值 | MVP 规则 |
| --- | --- | --- |
| 今日主线 | 降低选择负担，让今天有明确方向 | 每天只能标记 1 个任务为主线；完成时给予额外 XP 与视觉反馈 |
| 任务系统 | 每天知道要做什么、完成什么 | 支持每日任务、习惯、一次性任务；新增、编辑、删除、完成/取消完成 |
| XP 与等级 | 把小行动转为累计反馈 | 每任务有 XP；升级阈值固定；完成与撤销即时重算 |
| 五项属性 | 让用户看见生活是否偏科 | Vitality、Focus、Connection、Growth、Order；任务可关联一个属性 |
| 连续记录 | 强化规律，而非连续打卡焦虑 | 当天至少完成 1 个任务即延续；断开后重新开始，不扣分 |
| 周度雷达图 | 一眼发现失衡 | 展示最近 7 天各属性获得的 XP，相对归一化为 0–100 |
| 本周洞察 | 把统计变成下一步行动 | 每周显示一个最弱属性或明显趋势，并给出一条温和建议 |
| 本地存储 | 刷新后数据仍在 | localStorage + 版本化 schema + 安全的默认数据回退 |
| 设置与数据管理 | 用户有控制权 | 重置演示数据、导出 JSON、导入 JSON |

### P1：MVP 验证后加入

| 功能 | 说明 |
| --- | --- |
| 成就系统 | 例如首次 7 日连续、完成 50 个任务、单属性达到 1000 XP |
| 每周回顾 | 完成率、最高属性、最被忽略的属性，以及一条温和建议 |
| 任务模板 | “晨间启动”“深度工作”“每周整理”等一键加入 |
| 日历历史 | 查看过去每天任务与 XP |
| 深浅色主题与个性化 | 配色、昵称、头像、属性命名 |

### P2：明确暂不做

- 账号、云同步、多人协作
- AI 教练或聊天功能
- 社交排名、公开对比、强制排行榜
- 虚拟商城、装备、战斗、抽卡和扣血机制
- 复杂财务、健康设备同步、日程同步
- 移动原生 App

这些能力会增加复杂度，且在没有验证核心留存前不应进入首版。

## 5. 核心规则

### 5.1 任务类型与今日主线

| 类型 | 用途 | 示例 |
| --- | --- | --- |
| 每日任务 | 有明确重复频率的日常行动 | 晨间散步、计划明天 |
| 习惯 | 想持续培养的行为，不要求每天完成 | 阅读、联系朋友 |
| 一次性任务 | 完成后结束的具体事项 | 整理桌面、提交作品 |

- 每个任务都可关联一个人生属性与 XP 值。
- 用户每天可将其中 **一个** 任务标记为“今日主线”。
- 今日主线用于聚焦，不用于制造压力；未完成时不会产生惩罚。

### 5.2 XP 与等级

- 每个任务有 10、20、30 或 50 XP；完成今日主线额外获得 10 XP。
- 完成任务时，XP 计入总 XP 与对应属性。
- 取消完成时，XP 同步撤销。
- 初版采用固定公式：`level = floor(totalXP / 500) + 1`。
- 等级是正反馈，不解锁限制内容，也不会因漏做任务下降。

### 5.3 属性

| 属性 | 含义 | 示例任务 |
| --- | --- | --- |
| Vitality（活力） | 身体、睡眠、能量 | 运动、早睡、散步 |
| Focus（专注） | 心智、创造、深度工作 | 写作、编程、阅读 |
| Connection（连接） | 关系、陪伴、表达 | 打电话、约朋友、感谢某人 |
| Growth（成长） | 学习、勇气、探索 | 学习新技能、公开输出 |
| Order（秩序） | 环境、财务、生活管理 | 收纳、预算、规划明天 |

属性值不是人为填写的“自评分数”，而是由近期已完成任务的 XP 自动计算。这能避免用户为维护面板而额外劳动。

### 5.4 连续记录

- 完成当天任意一项任务，视为活跃日。
- 今天首次完成任务时更新连续天数。
- 若上一个活跃日不是昨天，连续数从 1 开始。
- 不补卡、不消耗道具；先保持系统简单可信。

### 5.5 周度洞察

- 每周根据最近 7 天的属性 XP 生成一条固定规则的洞察，无需 AI。
- 优先识别：最低属性、某属性连续 3 天无投入、任务完成率的明显变化。
- 文案保持支持性，例如：“这周你投入了很多专注时间。明天可试着安排一次 10 分钟的 Connection 任务。”
- 洞察只给一个，避免信息过载或评价用户的人生。

## 6. 信息架构与页面

### Dashboard（首页）

首屏必须可操作，按优先级排列：

1. 今日主线任务：最醒目，提供完成入口。
2. 当前等级、XP 与今日获得 XP。
3. 其余今日任务：完成、添加、编辑、删除。
4. 当前连续记录。
5. 五项属性进度与本周雷达图。
6. 一条本周洞察和可执行的小建议。

### History（历史，P1）

- 日历或时间线。
- 某日的完成任务、XP 与属性分布。

### Achievements（成就，P1）

- 100 个可计算成就，按入门、进阶、挑战、传奇四档难度分级。
- 已解锁与下一项接近解锁的成就，可按难度筛选。

### Settings（设置，P0）

- 昵称。
- 导出 / 导入数据。
- 恢复演示数据、清空所有数据（二次确认）。

## 7. 数据模型（localStorage）

键名：`life-dashboard:v1`

```ts
type Attribute = "vitality" | "focus" | "connection" | "growth" | "order";

type Task = {
  id: string;
  title: string;
  kind: "daily" | "habit" | "one_off";
  xp: 10 | 20 | 30 | 50;
  attribute: Attribute;
  date: "YYYY-MM-DD";
  isMainQuest: boolean;
  completedAt: string | null;
  createdAt: string;
};

type Profile = {
  name: string;
  createdAt: string;
};

type AppData = {
  version: 1;
  profile: Profile;
  tasks: Task[];
};
```

**派生数据不存储：** 总 XP、等级、连续天数、属性分、雷达图数据都从 `tasks` 计算，避免状态不一致。

## 8. 交互与状态要求

- 点击任务的完成状态，XP、等级进度、属性条、今日完成数在同一操作内更新。
- 新任务必须指定标题、类型、XP 和属性；默认值为习惯 + 20 XP + Focus。
- 标记今日主线时自动取消当天其他任务的主线状态。
- 空状态要引导用户添加第一个任务，而不是显示空白卡片。
- localStorage 损坏或旧版本数据无法读取时，保留原始备份键并回退到安全默认数据。
- 导入 JSON 前校验 schema；失败时显示清晰错误，不覆盖当前数据。

## 9. 成功指标

MVP 不追求下载量，先验证习惯价值：

- 首日：用户成功新增或完成至少 1 个任务。
- 第 3 天：仍有任务完成记录。
- 第 7 天：至少完成 3 个活跃日。
- 定性反馈：用户是否能说出“它帮我看见了什么”或“让我今天做了什么”。

## 10. 开发路线图

### Day 1：可用的 Dashboard UI（已完成基础视觉）

完成布局、响应式风格、等级、属性、任务、雷达图的视觉框架。

### Day 2：真实任务与 localStorage

- 用统一 `Task` 数据模型替代演示数据。
- 新增、编辑、删除、完成任务；支持三种任务类型与今日主线。
- 完整保存、恢复与安全回退。

### Day 3：XP、等级、属性与连续记录计算

- 以任务记录派生所有核心数字。
- 实现日期边界、撤销完成、周度属性聚合。

### Day 4：设置与数据控制（已完成）

- 昵称、导入、导出、恢复演示数据、清空数据。

### Day 5：成就与每周回顾（已完成）

- 100 个分级成就（入门、进阶、挑战、传奇）与按难度筛选。
- 每周回顾独立为单独 Tab，避免与成就列表争夺注意力。
- 一张可读、可行动的周度总结卡。

### Day 6：移动端体验与打磨（已完成）

- 任务操作优先，移动端布局、动效、无障碍与空状态。

### Day 7：发布与反馈（已完成发布准备）

- 公开访问版本、简短使用说明、收集真实用户反馈。

## 11. 下一步决策

下一次开发应从 **Day 2：真实任务与 localStorage** 开始；先让“任务类型 + 今日主线 + 本地数据”可靠运行，再实现 XP、属性和周度洞察。当前版本只有展示型 localStorage，尚未达到 MVP 标准。
